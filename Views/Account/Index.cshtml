@model InventoryControlSystem.ViewModels.AccountViewModel


<div class="container-fluid">
    <div class="row">
        <div class="col-md-3">
            <!-- Profile Image -->
            <div class="card card-primary card-outline">
                <div class="card-body box-profile">
                    @*<div class="text-center">
            @if (@Model.User.AccountImageString == null && @Model.User.AccountImageArray == null)
            {
                <img class="profile-user-img img-fluid img-circle" src="@Model.User.AccountImageDefault" alt="User profile picture">
            }
            else
            {
                <img class="profile-user-img img-fluid img-circle" src="data:image;base64,@Model.User.AccountImageString" alt="User profile picture">
            }
        </div>*@
                    <div class="text-center">
                        <img class="profile-user-img img-fluid img-circle" src="@Model.User.Picture" alt="User profile picture">
                    </div>
                    @*<img src="@Model.User.Picture" />*@
                    <h3 class="profile-username text-center">@Model.User.FirstName @Model.User.LastName</h3>

                    <p class="text-muted text-center">@Model.User.Role</p>

                    <b>ID: </b> @Model.User.ID
                    <br />
                    <b>Email: </b> @Model.User.Email
                </div>
                <!-- /.card-body -->
            </div>
            <!-- /.card -->
        </div>
        <!-- /.col -->
        <div class="col-md-9">
            <!-- About Me Box -->
            <div class="card card-primary">
                <div class="card-header">
                    <h3 class="card-title">About Me</h3>
                    <div class="card-tools">
                        <a asp-controller="User" asp-action="Edit" asp-route-id="@Model.User.Id" class="btn btn-sm btn-secondary">
                            Update Details
                        </a>
                        <button type="button" class="btn btn-tool" data-card-widget="collapse" data-toggle="tooltip" title="Collapse">
                            <i class="fas fa-minus"></i>
                        </button>
                        <button type="button" class="btn btn-tool" data-card-widget="remove" data-toggle="tooltip" title="Remove">
                            <i class="fas fa-times"></i>
                        </button>
                    </div>
                </div>
                <!-- /.card-header -->
                <div class="card-body">
                    <strong><i class="fas fa-book mr-1"></i> Phone</strong>
                    <p class="text-muted">@Model.User.Phone</p>

                    <hr>
                    <strong><i class="fas fa-book mr-1"></i> Address</strong>
                    <p class="text-muted">@Model.User.Address</p>

                    <hr>
                    <strong><i class="fas fa-book mr-1"></i> DOB</strong>
                    <p class="text-muted">@Model.User.DOB</p>

                    @*<hr>

                    <strong><i class="fas fa-map-marker-alt mr-1"></i> Location</strong>
                    <p class="text-muted">@Model.User.Location</p>

                    <hr>

                    <strong><i class="fas fa-pencil-alt mr-1"></i> DOB</strong>
                    <p class="text-muted">@Model.User.DOB</p>*@

                </div>
                <!-- /.card-body -->
            </div>
        </div>
    </div>
    <!-- /.row -->
</div><!-- /.container-fluid -->
